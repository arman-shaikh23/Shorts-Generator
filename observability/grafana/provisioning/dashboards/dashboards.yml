apiVersion: 1

providers:
  - name: "reelforge-dashboards"
    orgId: 1
    folder: "ReelForge"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 15
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

